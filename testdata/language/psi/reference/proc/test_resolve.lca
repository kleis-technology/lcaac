package language.psi.reference.proc.test_resolve

import language.psi.reference.proc.test_resolve.carrot
import language.psi.reference.proc.test_resolve.salad

process p {
    products {
        1 kg a
    }
    inputs {
        1 kg carrot from carrot_production()
    }
}
